{
  "name": "knitter-server",
  "version": "0.1.0",
  "description": "A single-user server for the Knitter app.",
  "main": "build/index.js",
  "scripts": {
    "start": "npm run build && node build/index.js",
    "dev": "npx nodemon",
    "build": "rimraf ./build && tsc"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.11.18",
    "nodemon": "^2.0.20",
    "rimraf": "^4.0.6",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4"
  }
}
